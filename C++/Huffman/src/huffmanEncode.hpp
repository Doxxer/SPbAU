#ifndef HUFFMANENCODE_HPP
#define HUFFMANENCODE_HPP

#include <string>

void Compress(std::string const &inputFileName, std::string const &outputFileName);

#endif /* end of include guard: HUFFMANENCODE_HPP */
